<div class="cms_border">
<h1>The body of the <%= @page.title %> sub-page will look like:</h1>
<%= raw(@page.body) %>
<p>
	<%= link_to 'Edit', edit_category_page_path(@page) %>
</p>
</div>
<%= link_to 'Back', category_path(@category) %>
