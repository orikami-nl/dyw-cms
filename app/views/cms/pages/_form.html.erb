<%= form_for([@category, @page]) do |f| %>
  <% if @page.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@page.errors.count, "error") %> prohibited this page from being saved:</h2>
      <ul>
      <% @page.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :position %><br />
    <%= f.text_field :position %>
  </div>
  <div class="field">
    <%= f.label :page_title %><br />
    <%= f.text_field :page_title %>
  </div>
  <div class="field">
    <%= f.label :link_name %><br />
    <%= f.text_field :link_name %>
  </div>
  <div class="field">
    <%= f.label :body %><br />
    <%= f.text_area :body, :id => "input", :name => "input" %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
