<table>
  <tr>
    <th>Name</th>
    <th>File Url</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @assets.each do |file| %>
  <tr>
    <td><%= file.name %></td>
    <td><%= file.file_url %>
    <td><%= link_to 'Show', file.file_url %></td>
    <td><%= link_to 'Edit', edit_file_path(file) %></td>
    <td><%= link_to 'Destroy', file, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Asset', new_file_path %>
